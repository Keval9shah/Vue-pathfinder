name: create_build

on:
  push:
    branches: 'main'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:

    - name: Build
      run: |
        cd pathfinder
        npm install
        npm run build
